# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build example-angular-app`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t example-angular-app`.
FROM docker.io/node:lts-alpine@sha256:a7b980c958bfe4d84ee9263badd95a40c8bb50ad5afdb87902c187fefaef0e24

# TODO: move to caddy
# docker run -d -p 80:80 \
# -v $PWD/index.html:/usr/share/caddy/index.html \
# -v caddy_data:/data \
# caddy

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system example-angular-app && \
  adduser --system -G example-angular-app example-angular-app

COPY dist/apps/example-angular-app example-angular-app
RUN chown -R example-angular-app:example-angular-app .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix example-angular-app --omit=dev -f install

CMD [ "node", "example-angular-app" ]
